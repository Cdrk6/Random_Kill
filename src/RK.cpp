#include <iostream>
#include "RK.hpp"
#include "Map.hpp"
#include "Input.hpp"

using namespace std;

int main(int argc, char* args[]) {
    cout << "Hello World\n";
    //start();
    Input *myInput = new Input(5);
    return 0;
}
