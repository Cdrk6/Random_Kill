#ifndef ENTITY_HPP
#define ENTITY_HPP

class Entity {
public:
    Entity();
    Entity(const Entity& orig);
    virtual ~Entity();
    //virtual void overrideMe();
    
private:

};

#endif /* ENTITY_HPP */

